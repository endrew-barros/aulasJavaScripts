const mensagem = 'ola End'
const sobrenome = 'Barros'
const names  = 'en,endrew,ale'
let number  = 6
// const texto = number.toString() // tranforma um tipo em string

//Tamanho da variavel
//console.log(mensagem.length)

//forma mais melhor para a ligacao de duas variaveis em texto /// maisuscula e forma minuscula
// console.log(`${mensagem.toUpperCase} ${sobrenome.toLowerCase}`)//se chama template strings

//tranformar uma string em uma lista
// console.log(names.split(','))

console.log(number = number.toString())
console.log(typeof number)